{
  "name": "@cloudcache/app",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsup",
    "build:bundle": "tsup",
    "dev": "pnpm exec wrangler dev --port 8789"
  },
  "dependencies": {
    "@cloudcache/platform-env": "workspace:*",
    "@cloudcache/platform-logging": "workspace:*",
    "@cloudcache/platform-http": "workspace:*",
    "@cloudcache/platform-crypto": "workspace:*",
    "@cloudcache/platform-validate": "workspace:*"
  },
  "devDependencies": {
    "tsup": "^8.2.2",
    "typescript": "^5.6.0"
  }
}
