{
  "name": "Cloudcache",
  "private": true,
  "scripts": {
    "build": "tsup src/index.ts --format esm",
    "dev": "pnpm build && pnpm exec wrangler dev --port 8788 --inspector-port 9231 --no-bundle"
  },
  "devDependencies": {
    "tsup": "^8.2.2"
  }
}
