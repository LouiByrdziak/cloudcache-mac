{
  "name": "Cloudcache",
  "private": true,
  "scripts": {
    "build": "tsup src/index.ts --format esm",
    "dev": "pnpm exec wrangler dev --port 8788"
  },
  "devDependencies": {
    "tsup": "^8.2.2"
  }
}
