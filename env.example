# =============================================================================
# Cloudflare Zone API Credentials
# =============================================================================
# These are used by:
# 1. CI/CD scripts (deploy-module.sh, cloudcache script) - stored in GitHub Secrets
# 2. APP Worker runtime - for toggle sync feature (Rocket Loader, Brotli, etc.)
#
# The APP module calls the Cloudflare Zone Settings API at runtime to sync toggles.
# These secrets MUST be added to the app-worker using: scripts/cloudcache bind app <env>
#
CF_API_TOKEN="your_cloudflare_api_token"
CF_ACCOUNT_ID="your_cloudflare_account_id"
CF_ZONE_ID="your_cloudflare_zone_id"

# =============================================================================
# Cloudflare Access Credentials
# =============================================================================
# Service token for Cloudflare Access authentication
# Runtime secrets - stored in Cloudflare Workers/Pages environments
#
CF_ACCESS_CLIENT_ID="your_access_client_id"
CF_ACCESS_CLIENT_SECRET="your_access_client_secret"

# =============================================================================
# Shopify API Credentials
# =============================================================================
# Shopify OAuth app credentials (APP module only)
# Runtime secrets - stored in Cloudflare Workers environments
#
SHOPIFY_API_KEY="your_shopify_api_key"
SHOPIFY_API_SECRET="your_shopify_api_secret"




